# web page built with Angular 2.x

Webpage in master branch will be the only visible

# copy other branches to master 'hard reset'
git reset --hard other_branch

# force sending changes 
git push -f origin master 

# push all tags and branches
git push --all origin

# pull all tags and branches
git pull --all origin
